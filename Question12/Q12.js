"use strict";
// Storing names of friends in array
let names = ["Saleem", "Sadiq", "Rehan", "Kamal"];
// Printing a personalized message to each friend
for (let i = 0; i < names.length; i++) {
    console.log(`Hello ${names[i]}, Good Morning!`);
}
