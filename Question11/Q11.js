"use strict";
// Storing names of friends in an array called names
let names = ["Atif", "Sumair", "Amir", "Adnan"];
// Printing each name by accessing each element in the list, one at a time
for (let i = 0; i < names.length; i++) {
    console.log(names[i]);
}
